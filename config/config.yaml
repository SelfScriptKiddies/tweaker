server:
  host: "0.0.0.0"
  port: 8080

log:
  level: "info"          # debug, info, warn, error
  format: "console"      # console or json

database:
  host: "db.example.com"
  port: 5432
  user: "myuser"
  pass: "mypassword"
  name: "mydb"
  sslmode: "disable"

auth:
  username: "admin"
  password: "admin"
  secret_cookie: "authorized"
